{
  "name": "gateway",
  "version": "1.0.0",
  "private": true,
  "description": "API Gateway for the Uber System microservices demo (reverse proxy to user/captain/ride services).",
  "type": "module",
  "main": "app.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "nodemon app.js",
    "start": "node app.js",
    "test": "node --test",
    "test:all": "npm --prefix ../user test && npm --prefix ../captain test && npm --prefix ../ride test && npm --prefix ../gateway test"
  },
  "keywords": [
    "uber-system",
    "microservices",
    "api-gateway",
    "express",
    "proxy"
  ],
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "express-http-proxy": "^2.1.2",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "supertest": "^7.1.4"
  }
}
